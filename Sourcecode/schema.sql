DROP TABLE if EXISTS users;

CREATE TABLE users (
   username text, 
   password text
);

DROP TABLE if EXISTS insults;

Create Table insults (
   username text,
   insult text,
   tag1 text,
   tag2 text,
   tag3 text,
   tag4 text,
   tag5 text,
   tag6 text,
   likes text,
   dislikes text
);
